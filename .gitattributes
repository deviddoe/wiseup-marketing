# Keep line endings identical on Windows, macOS and Linux.
# Without this, a Windows clone gets CRLF working copies and every later edit
# looks like a whole-file change in the diff.
* text=auto eol=lf

# Binary assets - never touch these.
*.png binary
*.jpg binary
*.jpeg binary
*.pdf binary
*.woff binary
*.woff2 binary
*.ttf binary
